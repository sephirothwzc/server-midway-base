type User {
  _id: ID
  userName: String!
  password: String!
}

extend type Query {
  user(id: ID): [User]!
}

extend type Mutation {
  userSave(param: UserSaveIn): String
}

input UserSaveIn {
  _id: ID
  userName: String!
  password: String!
}
